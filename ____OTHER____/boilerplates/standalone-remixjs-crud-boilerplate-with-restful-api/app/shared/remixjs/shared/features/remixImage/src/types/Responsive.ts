export interface Responsive {
  /** The maximum width that the image element can have. */
  maxWidth: number | undefined;
  /** The size of the image element. */
  size: {
    width: number;
    height: number;
  };
}
